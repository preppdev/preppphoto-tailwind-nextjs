import { CheckCircleIcon, PhoneIcon } from '@heroicons/react/outline'

const callsToAction = [
  // {watch demo} in Services Menu so that it can open as a modal
  { name: 'View Samples', href: '/samples', icon: CheckCircleIcon },
  { name: 'Contact Us', href: '/contact', icon: PhoneIcon },
]

module.exports = callsToAction
